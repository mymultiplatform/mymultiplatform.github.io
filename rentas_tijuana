<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Formulario de Registro / Solicitud</title>
  <style>
    :root{--bg:#f6f8fa;--card:#ffffff;--muted:#6b7280;--accent:#1f6feb}
    body{font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial; background:var(--bg); padding:28px;}
    .container{max-width:780px;margin:24px auto;padding:22px;background:var(--card);border-radius:12px;box-shadow:0 6px 20px rgba(20,20,30,0.06)}
    h1{font-size:20px;margin:0 0 12px}
    p.lead{margin:0 0 18px;color:var(--muted)}
    form{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}
    label{display:block;font-size:13px;margin-bottom:6px}
    input[type=text], input[type=email], input[type=tel], input[type=password], input[type=file], input[type=date], select, textarea{
      width:100%;padding:10px;border:1px solid #e6e9ee;border-radius:8px;font-size:14px;box-sizing:border-box
    }
    .full{grid-column:1 / -1}
    .actions{display:flex;gap:8px;align-items:center;margin-top:12px}
    button{padding:10px 14px;border-radius:10px;border:0;background:var(--accent);color:white;font-weight:600;cursor:pointer}
    button.ghost{background:transparent;color:var(--muted);border:1px solid #e6e9ee}
    .small{font-size:13px;color:var(--muted);margin-top:6px}
    .file-meta{margin-top:6px;font-size:13px;color:var(--muted)}
    .terms{display:flex;align-items:center;gap:8px}
    @media (max-width:720px){form{grid-template-columns:1fr}}
  </style>
</head>
<body>
  <div class="container" role="main">
    <h1>Formulario de Registro / Solicitud</h1>
    <p class="lead">Rellena los campos para completar tu registro. Los campos marcados con * son obligatorios.</p>

    <form id="registrationForm" action="#" method="post" enctype="multipart/form-data" novalidate>
      <div>
        <label for="firstName">Nombre *</label>
        <input id="firstName" name="firstName" type="text" required autocomplete="given-name" placeholder="Ej. María">
      </div>

      <div>
        <label for="lastName">Apellido *</label>
        <input id="lastName" name="lastName" type="text" required autocomplete="family-name" placeholder="Ej. González">
      </div>

      <div class="full">
        <label for="address">Dirección *</label>
        <input id="address" name="address" type="text" required autocomplete="street-address" placeholder="Calle, número, piso, etc.">
      </div>

      <div>
        <label for="city">Ciudad *</label>
        <input id="city" name="city" type="text" required placeholder="Ej. Madrid">
      </div>

      <div>
        <label for="state">Estado / Provincia</label>
        <input id="state" name="state" type="text" placeholder="Ej. Comunidad de Madrid">
      </div>

      <div>
        <label for="postal">Código Postal</label>
        <input id="postal" name="postal" type="text" inputmode="numeric" pattern="[0-9]{4,10}" placeholder="Ej. 28001">
      </div>

      <div>
        <label for="country">País *</label>
        <input id="country" name="country" type="text" required placeholder="Ej. España" autocomplete="country">
      </div>

      <div>
        <label for="email">Correo Electrónico *</label>
        <input id="email" name="email" type="email" required autocomplete="email" placeholder="ejemplo@correo.com">
      </div>

      <div>
        <label for="phone">Número de Teléfono</label>
        <input id="phone" name="phone" type="tel" inputmode="tel" placeholder="+34 600 000 000">
      </div>

      <div>
        <label for="dob">Fecha de Nacimiento</label>
        <input id="dob" name="dob" type="date" max="2025-11-06">
      </div>

      <div>
        <label for="username">Nombre de Usuario *</label>
        <input id="username" name="username" type="text" required placeholder="usuario123" autocomplete="username">
      </div>

      <div>
        <label for="password">Contraseña *</label>
        <input id="password" name="password" type="password" required minlength="8" placeholder="Mín. 8 caracteres" autocomplete="new-password">
      </div>

      <div>
        <label for="confirmPassword">Confirmar Contraseña *</label>
        <input id="confirmPassword" name="confirmPassword" type="password" required minlength="8" placeholder="Repite la contraseña" autocomplete="new-password">
      </div>

      <div class="full">
        <label for="file">Adjuntar Archivo (PDF, CV, Documento) — Máx. 5 MB</label>
        <input id="file" name="file" type="file" accept="application/pdf" aria-describedby="fileHelp">
        <div id="fileHelp" class="file-meta">Ningún archivo seleccionado</div>
      </div>

      <div class="full terms">
        <input id="terms" name="terms" type="checkbox" required>
        <label for="terms">Acepto los Términos y Condiciones *</label>
      </div>

      <div class="full actions">
        <button type="submit">Enviar</button>
        <button type="button" class="ghost" id="clearBtn">Borrar Formulario</button>
        <div style="margin-left:auto;color:var(--muted);font-size:13px">* Campos obligatorios</div>
      </div>
    </form>
  </div>

  <script>
    (function(){
      const form = document.getElementById('registrationForm');
      const fileInput = document.getElementById('file');
      const fileHelp = document.getElementById('fileHelp');
      const clearBtn = document.getElementById('clearBtn');

      // Mostrar nombre y tamaño del archivo seleccionado
      fileInput.addEventListener('change', () => {
        const f = fileInput.files[0];
        if(!f){ fileHelp.textContent = 'Ningún archivo seleccionado'; return; }
        if(f.type !== 'application/pdf'){
          fileHelp.textContent = 'Solo se permiten archivos PDF.';
          fileInput.value = '';
          return;
        }
        const sizeMB = (f.size / (1024*1024)).toFixed(2);
        if(f.size > 5 * 1024 * 1024){
          fileHelp.textContent = `El archivo es demasiado grande (${sizeMB} MB). Máx. 5 MB.`;
          fileInput.value = '';
          return;
        }
        fileHelp.textContent = `Seleccionado: ${f.name} — ${sizeMB} MB`;
      });

      // Limpiar formulario
      clearBtn.addEventListener('click', () => { form.reset(); fileHelp.textContent = 'Ningún archivo seleccionado'; });

      // Validación de contraseña coincidente
      form.addEventListener('submit', (e) => {
        const pw = document.getElementById('password').value;
        const cpw = document.getElementById('confirmPassword').value;
        if(pw !== cpw){
          e.preventDefault();
          alert('Las contraseñas no coinciden. Por favor, revísalas.');
          return false;
        }

        // Aquí podrías enviar el formulario con fetch/XHR si no deseas recargar.
        // e.preventDefault();
        // En este ejemplo dejamos que el formulario se envíe de forma tradicional.
      });
    })();
  </script>
</body>
</html>
